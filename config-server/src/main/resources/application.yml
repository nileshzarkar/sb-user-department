server:
  port: 8088

# spring:
#   profiles:
#     active: native
#   cloud:
#     config:
#       server:
#         bootstrap: true

spring:
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nileshzarkar/config.git
          clone-on-start: true
          default-label: main
